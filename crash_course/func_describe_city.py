def describe_ciry(city_name, country_name='Sweden'):
    print(f"{city_name.title()} is in {country_name.title()}")

describe_ciry(city_name='Stockholm')

describe_ciry(city_name='London', country_name='England')

describe_ciry('Berlin', 'Germany')